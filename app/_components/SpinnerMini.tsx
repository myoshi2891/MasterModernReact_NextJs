/**
 * Renders a minimal spinner used to indicate loading state.
 *
 * @returns A JSX element — a <div> with className "spinner-mini".
 */
function SpinnerMini() {
	return <div className="spinner-mini"></div>;
}

export default SpinnerMini;